{
  "builds": [
    {
      "src": "project/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "project/wsgi.py"
    }
  ],
  "buildCommand": "python manage.py migrate --noinput --settings=project.settings && python manage.py collectstatic --noinput --settings=project.settings"
}